@extends('layouts.tabler')
@section('title', 'Blog — PanduPOS')
@section('header', 'Marketing · Blog')
@section('content')
<div class="row row-cards"><div class="col-lg-5"><div class="card"><div class="card-header"><h3 class="card-title">Artikel baru</h3></div><div class="card-body"><form method="POST" action="{{ route('platform.blog.store') }}">@csrf
<div class="mb-3"><label class="form-label">Judul</label><input class="form-control" name="title" required></div><div class="mb-3"><label class="form-label">Slug (opsional)</label><input class="form-control" name="slug"></div><div class="mb-3"><label class="form-label">Kategori</label><select class="form-select" name="category_id"><option value="">Tanpa kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></div><div class="mb-3"><label class="form-label">Ringkasan</label><textarea class="form-control" name="excerpt" rows="3"></textarea></div><div class="mb-3"><label class="form-label">Konten</label><textarea class="form-control font-monospace" name="content" rows="12" required></textarea></div><div class="mb-3"><label class="form-label">URL gambar unggulan</label><input class="form-control" name="featured_image" type="url"></div><div class="mb-3"><label class="form-label">Meta title</label><input class="form-control" name="meta_title"></div><div class="mb-3"><label class="form-label">Meta description</label><textarea class="form-control" name="meta_description" rows="2"></textarea></div><label class="form-check mb-3"><input class="form-check-input" type="checkbox" name="is_published" value="1"><span class="form-check-label">Publikasikan sekarang</span></label><button class="btn btn-primary w-100">Simpan artikel</button></form></div></div></div>
<div class="col-lg-7"><div class="card"><div class="card-header"><h3 class="card-title">Daftar artikel</h3></div><div class="table-responsive"><table class="table table-vcenter"><thead><tr><th>Artikel</th><th>Status</th><th>Publikasi</th><th></th></tr></thead><tbody>@forelse($posts as $post)<tr><td><strong>{{ $post->title }}</strong><div class="text-secondary">{{ $post->category?->name ?? 'Tanpa kategori' }}</div></td><td><span class="badge {{ $post->is_published ? 'bg-green-lt' : 'bg-secondary-lt' }}">{{ $post->is_published ? 'Terbit' : 'Draft' }}</span></td><td>{{ $post->published_at?->format('d M Y H:i') ?? '—' }}</td><td><div class="d-flex gap-2">@if($post->is_published)<a class="btn btn-sm btn-outline-primary" href="{{ route('blog.show', $post->slug) }}">Lihat</a>@endif<form method="POST" action="{{ route('platform.blog.destroy',$post) }}" onsubmit="return confirm('Hapus artikel ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus</button></form></div></td></tr>@empty<tr><td colspan="4" class="text-center text-secondary py-5">Belum ada artikel.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $posts->links() }}</div></div></div></div>
@endsection
