<!DOCTYPE html><html lang="id"><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;color:#17231d;font-size:11px}h1{font-size:22px;margin:0 0 4px}.meta{color:#65736b;margin-bottom:20px}.stats{width:100%;margin-bottom:20px}.stats td{border:1px solid #dce3de;padding:10px}.stats strong{display:block;font-size:16px;margin-top:5px}table.data{width:100%;border-collapse:collapse}.data th,.data td{border:1px solid #dce3de;padding:7px;text-align:left}.data th{background:#eef4ef;text-transform:uppercase;font-size:9px}footer{position:fixed;bottom:0;color:#78847d;font-size:9px}</style></head><body>@php($currencyLabels = ['Omzet', 'Rata-rata transaksi', 'Lunas', 'Pendapatan', 'COGS', 'Laba kotor', 'Nilai pembelian', 'Valuasi stok'])<h1>{{ $title }}</h1><div class="meta">{{ $tenant->name }} · {{ $from }} sampai {{ $to }} · {{ ucfirst($group) }}</div><table class="stats"><tr>@foreach($summary as $label=>$value)<td>{{ $label }}<strong>{{ str_contains($label, '%') ? number_format((float) $value, 1, ',', '.').'%' : (in_array($label, $currencyLabels, true) ? 'Rp '.number_format((float) $value, 0, ',', '.') : number_format((float) $value, 0, ',', '.')) }}</strong></td>@endforeach</tr></table><table class="data"><thead><tr>@foreach($columns as $column)<th>{{ $column }}</th>@endforeach</tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($row as $value)<td>{{ is_float($value) ? number_format($value,2,',','.') : $value }}</td>@endforeach</tr>@empty<tr><td colspan="{{ count($columns) }}">Tidak ada data.</td></tr>@endforelse</tbody></table><footer>Dibuat oleh PanduPOS pada {{ now()->format('d-m-Y H:i') }}</footer></body></html>
